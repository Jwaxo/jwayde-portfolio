{#
/**
 * @file
 * Theme override for the thumbnail field in a teaser display.
#}
{% extends 'field--inline.html.twig' %}

{% block field_prefix %}
  <span class="title">
    <h4>{{ element['#object'].title.value }}</h4>
    <span class="button button--cta">{{ "Click to learn more"|t }}</span>
  </span>
{% endblock %}
